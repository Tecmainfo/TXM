<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- Exemple de style pour bouton principal -->
    <Style TargetType="Button" x:Key="BoutonPrincipal" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- Exemple de style pour TextBox -->
    <Style TargetType="TextBox" x:Key="ChampTexte">
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="materialDesign:HintAssist.Foreground" Value="Gray"/>
    </Style>

</ResourceDictionary>
